name: fitness_tracker
description: A calorie tracking mobile application MVP - similar to MyFitnessPal.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # HTTP Networking
  http: ^1.2.0

  # AWS Amplify for Cognito Auth
  amplify_flutter: ^1.6.1
  amplify_auth_cognito: ^1.6.1

  # Local Storage
  shared_preferences: ^2.2.2

  # UI Components
  cupertino_icons: ^1.0.6
  fl_chart: ^0.66.2
  intl: ^0.20.0

  # Image Picker
  image_picker: ^1.0.7

  # UUID generation
  uuid: ^4.3.3

  # Form validation
  # flutter_form_builder: ^9.3.0
  # form_builder_validators: ^10.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
